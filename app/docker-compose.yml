services:
  percepta:
    build: .
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    restart: unless-stopped
    networks:
      - secure_web_net

networks:
  secure_web_net:
    external: true
