1. **image14** — used to show how a serif typeface makes a difference. Typography plays a huge part in determining how a design feels. If you want an elegant or classic look, you might want to incorporate a serif typeface in your design. show this image only if we find font that is not that regular.

2. **image15** — used to show how a rounded sans-serif typeface makes a difference. Typography plays a huge part in determining how a design feels. For a playful look, you could use a rounded sans serif.show this image only if we find font that is not that regular.

3. **image16** — used to show how a neutral sans-serif typeface makes a difference. Typography plays a huge part in determining how a design feels. If you're going for a plainer look, or want to rely on other elements to provide the personality, a neutral sans serif works great.show this image only if we find font that is not that regular.

4. **image17** — remove

5. **image18** — remove


6. **image19** — remove

7. **image23** — used to show how a languange makes a difference.  While not a visual design technique per se, the words you use in an interface have a massive influence on the overall personality.
Using a less personal tone might feel more official or professional.show this image only if the language of the text is formal.

8. **image24** — used to show how a casual friendly copy tone makes a difference. Using friendlier, more casual language makes a site feel, well, friendlier. Words are everywhere in a user interface, and choosing the right ones is just as (if not more) important than choosing the right color or typeface. show this image only if the language of the text is formal.

9. **image26** — delete

10. **image37** — used to show how colour lightness in secondary text makes a difference. Making text a lighter grey is a great way to de-emphasize it on white backgrounds, but it doesn’t look so great on colored backgrounds. That’s because the effect we’re actually seeing with grey on white is reduced
contrast.  show this together with image38,39,40,41, only if there is gray/gray-shaded text on colored background.

11. **image38** — used to show how grey text on a coloured card background makes a difference. Making the text closer to the background color is what actually helps create hierarchy, not making it light grey.

12. **image39** — used to show how the same grey value reads differently on white vs coloured surfaces. You might think that the easiest way to achieve this is to use white text and reduce the opacity. While this does reduce the contrast, it often results in text that looks dull,
washed out, and sometimes even disabled.

13. **image40** — used to show how grey secondary text on a coloured card creates unintended contrast. Even worse, using this approach on top of an image or pattern means the background will show through the text.

14. **image41** — used to show how hue-matched tinted text on a coloured card makes a difference. A better approach is to hand-pick a new color, based on the background color. Choose a color with the same hue, and adjust the saturation and lightness until it looks right to you. Hand-picking a color this way makes it easy to reduce the contrast without
the text looking faded.

15. **image42** — remove

16. **image43** — remove

17. **image44** — used to show how not using background color to every content area makes a difference. if a sidebar feels like it’s competing with your main content area, don’t give it a background color — let the content sit directly on the page background instead. show this image only if there is background color in content area.

18. **image51** — used to show how an oversized section heading harms hierarchy. A default large heading can outweigh the content it introduces — labels should be quieter than what they label. show this image only if heading size is big.

19. **image52** — used to show how reducing a heading's visual weight makes a difference. Reducing a section heading to small pale text lets the actual controls become the most prominent elements on the page.show this image only if heading size is big.

20. **image53** — used to show how hierarchy without prominent headings makes a difference. Strong hierarchy can exist without prominent headings when content uses size, weight, and spacing differences.show this image only if heading size is big.

21. **image54** — used to show how  icons (especially solid ones) are generally pretty “heavy” and cover a lot of surface area. As a result, when you put an icon next to some text, the icon tends to feel emphasized.. A small solid-filled icon has more visual surface area than nearby text characters, making it feel disproportionately heavy. show this image together with image55 only when there are icons next to text on the page.

22. **image55** — used to show how reducing icon fill or contrast balances it with text. Removing the fill or reducing the icon's contrast brings it into balance with adjacent text without changing its size. 

23. **image58** — used to show how equal-weight button styles harm action hierarchy. Three equal-weight filled buttons offer no signal about which action is primary — the user must read every label. shown together with image59. show this only if there are 2 or more buttons next to each with same weight inside of a container/label.

24. **image59** — used to show how button style hierarchy (filled/outlined/text) makes a difference. Using filled, outlined, and text-only button styles creates an instant primary → secondary → tertiary action hierarchy.

25. **image61** — used to show how limiting dominant actions per section makes a difference. Limiting each section to one dominant action prevents the page from feeling like every option matters equally. show this only if there is a button with destructive action, together with image62

26. **image62** — used to show how styling a destructive action as a plain text link makes a difference. A dangerous action styled as a plain text link is still findable but draws no alarming attention.

27. **image63** — used to show how overall form spacing makes a difference. Without breathing room between element groups, a form feels compressed and stressful to fill out. only show this if there is a element with a lot of text and if the spacing of text inside of an element is small.

28. **image64** — remove

29. **image65** — used to show how generous padding and gap makes a design feel more polished. The same content with significantly more padding and gap feels calmer and more professionally finished. show this image only with image63 with saying how to solve whitespace in element is  to start by giving something way too much space, then remove it until it you’re happy with the result.

30. **image72** — used to show how full-width stretched content makes a difference. Stretching content to fill the full browser width makes line lengths uncomfortable and the layout look sparse. show this only if the input elements are spread out too much, not counting nav bars. shown together with image73.

31. **image73** — used to show how Spreading things out or making things
unnecessarily wide just makes an interface harder to interpret.

32. **image77** — used to show how a single-column form on a wide screen wastes horizontal space. A single narrow form column on a wide screen wastes large amounts of horizontal space on both sides. only show this image if there is a form on the page. together with image78.

33. **image78** — used to show how a two-column form layout makes better use of wide screens. Reorganising the same fields into two columns makes better use of horizontal space without harming legibility.

34. **image90** — used to show how There’s nothing inherently wrong with using relative units like em, but don’t be fooled into believing that relationships defined this way can remain static — 2.5em might be the perfect headline size on desktop but there’s no guarantee that it’ll be the right size on smaller screens. shown together with image91 and image92. show this only if there is text with reletive units like "em"

35. **image91** — Say you reduce the size of your body copy to 14px on small screens to keep the line length in check. Keeping your headlines at 2.5em means a rendered font size of 35px — way too big for a small screen! show together with image90 and 92.

36. **image92** — A better headline size for small screens might be somewhere between 20px and 24px.

37. **image94** — used to show how proportional button padding makes a difference. Proportional em-based padding makes small, medium, and large buttons look like identical scaled copies of each other. show this only if there are small buttons in the page. together with image95.

38. **image95** — used to show how non-proportional button padding makes a difference. Non-proportional padding — more on large, less on small — gives each button size a distinct, deliberate character.

39. **image97** — used to show how label-to-input gap spacing makes a difference. When the gap between a label and its input equals the gap between two separate fields, nothing clusters as a unit. show this image only if there is label-to-input elements in the page. together with image98.

40. **image98** — used to show how label-to-input gap spacing makes a difference. Setting the label-to-input gap much smaller than the field-to-field gap makes each label visually belong to its own input.show this image only if there is label-to-input elements in the page. together with image97.

41. **image99** — used to show how spacing above and below a heading makes a difference. Equal space above and below a heading leaves it floating between sections, unclear whether it opens or closes a block. show this image only if there is an article on the page with multiple sections and headings.

42. **image100** — used to show when the space between bullets matches the lineheight of a single bullet.show this image only if there is bulleted list on the page.

43. **image102** — used to show how arbitrary font size choices harm type hierarchy. Arbitrary font sizes chosen one at a time produce a hierarchy that feels accidental and inconsistent. show this only if there is a container/bigger-element with combination of buttons, texts, labels, headings, where spacing between these elements is too random. show together with image 105.

44. **image105** — used to show how a defined type scale makes a difference. Restricting sizing to a defined scale makes the typographic hierarchy feel intentional and easy to scan.

45. **image107** — remove

46. **image112** — used to show how 90–110 character line length makes reading harder. Lines running 90–110 characters force the eye to travel far right and struggle to find the start of the next line. show this only if there is text with long lines, 90-110 chars. shown with image113 and image114.

47. **image113** — used to show how using A width of 20-35em for line length will get you in the right ballpark. shown with image 112 and  114.

48. **image114** — used to show how Going a bit wider than 75 characters per line can sometimes work too, but be aware that you’re entering risky territory — stick to the 45-75 range if you want to play it safe.

49. **image115** — used to show how splitting wide text into columns solves line-length problems. Breaking wide text into two or three columns solves over-long line lengths without removing any content. shown only if the paragraph text is mixed with images or other large components. together with image 116.

50. **image116** — used to show how we can use different widths in the same content area, but the result almost always looks more polished. together with image 115.

51. **image118** — remove

52. **image120** — remove

53. **image122** — used to show how When lines of text are spaced too tightly, it’s easy to finish reading a line of text at the right edge of a page then jump your eyes all the way back to the left edge only to be unsure which line is next. shown together with image123 only if there is a large paragraph with tight spacing.

54. **image123** — used to show how that your line-height and paragraph width should be proportional — narrow content can use a shorter line-height like 1.5, but wide content might need a line-height as tall as 2.

55. **image124** — used to show When text is small, extra line spacing is important because it makes it a lot easier for your eyes to find the next line when the text wraps. show this image only if there is small line spacing in paragraphs with 5+ lines.

56. **image125** — used to show how tight line-height on large display headings makes a difference. Large display text at tight 1.1× line-height feels natural — the eye does not need vertical space to return when lines are short. shown if the spacing of large headline is bigger than 1.

57. **image127** — used to show how styling links with weight and underline instead of colour makes a difference. Styling links with font weight and a subtle underline instead of colour alone signals interactivity without relying on link-blue. shown with image128 and 129, shown only if there is interface with line of elements where almost everything is a link.

58. **image128** — used to show how Instead, emphasize most links in a more subtle way, like by just using a heavier font weight or darker color.

59. **image129** — used to show how Some links might not even need to be emphasized by default at all. If you’ve got links in your interface that are really ancillary and not part of the main path a user takes through the application, consider adding an underline or changing the color only on hover.

60. **image132** — used to show how center alignment for short header pairs makes a difference. if something is longer than two or three lines, it will almost always look better left-aligned. show this image only if there is an elements with more than 2-3 lines and center-alignment.

61. **image133** — used to show how If you’ve got a few blocks of text you want to center but one of them is a bit too long, the easiest fix is to rewrite the content and make it shorter. show this image if there are 3+ elements side by side where one has 2 3 lines and others might have 6 7 lines of text.

62. **image134** — used to show how right-aligning numbers in a table column makes a difference. Right-aligning numbers in a table column lines up decimal points and digit positions for fast visual comparison. show this only if there is a table on page.

63. **image135** — remove

64. **image136** — remove

65. **image141** — used to show how letter-spacing on all-caps labels makes a difference. All-caps labels benefit from ~0.08em of added letter-spacing — uniform capitals need more width between them to be legible. show this image only if there is such text.

66. **image151** — used to show how a complete UI colour palette is structured. A real UI needs a full grey scale, a primary hue scale, and accent colours for info, warning, error, and success.To build something real, you need a much more comprehensive set of colors to choose from. show this if the whole UI has only 4-5 different colors. show together with image152.

67. **image152** — used to show how having only three grey shades falls short. Three grey shades look like enough until you try to cover every text level, border, background, and disabled state. show this image only if the UI has only 1-2 grays.shown together with image153.

68. **image153** — used to show how a full 9-shade grey scale makes a difference. A complete 9-shade grey scale covers every text level, surface, border, and disabled state a real UI needs. 

69. **image154** — used to show primary colors are the colors that determine the overall look of a site — the ones that make you think of Facebook as “blue”. Ultra-light shades can be useful as a tinted background for things like alerts, while darker shades work great for text. show this image only if the UI has 1-2 colors.

70. **image155** — used to show how semantic accent colours for status states make a difference. Separate accent colours for info, warning, error, and success let feedback states communicate meaning without ambiguity. show this image if there are no accent colors in UI. together with image156, 157, 158.

71. **image156** — used to show You might also need colors to emphasize different semantic states, like red for confirming a destructive action.shown only with when image155 is shown.


72. **image157** — You might also need colors to emphasize different semantic states, like yellow for a warning message. shown only with when image155 is shown.


73. **image158** — You might also need colors to emphasize different semantic states, like green to highlight a positive trend.shown only with when image155 is shown.


74. **image159** — remove

75. **image160** — remove

76. **image180** — used to show how adding cool blue saturation to grey makes it feel crisp. Adding a hint of blue saturation to grey makes it feel crisp and airy rather than lifeless. show this only if there is natural grays in UI. together with image181.

77. **image181** — used to show how adding warm yellow-orange saturation to grey makes it feel inviting. Adding a hint of yellow-orange saturation to grey makes it feel warm and inviting rather than cold.

78. **image183** — used to show how WCAG contrast ratio levels make a difference. There is a visible brightness gap between a failing 3.5:1 ratio, an AA-passing 5.4:1, and an AAA-passing 7.6:1. show this image only if there is such bad contrast of white text and colored background. together with image185.

79. **image185** — used to show how dark text on a light tinted background compares to white text on dark. Dark coloured text on a light tinted background is more legible and refined than white text on a dark colour fill. only with image183.

80. **image186** — used to show how white-on-dark text loses hierarchy when both levels are the same colour. When heading and body text are both white on a dark panel, text hierarchy completely disappears. shown this only if the primary text and the secondary text color look the same. together with image 187.

81. **image187** — used to show how hue-rotated secondary text on a dark panel restores hierarchy. Changing secondary body text to a slightly lighter hue-rotated version of the panel colour restores visible hierarchy while staying on-brand.

82. **image188** — used to show how colour-only status dots fail colourblind users. Status dots differentiated only by colour are indistinguishable to users who cannot perceive colour differences. show this image together with image189 only if there are metric cards with such colors used.

83. **image189** — used to show how adding icons alongside coloured status dots improves accessibility. Adding a matching icon alongside each coloured status dot makes the state readable regardless of colour perception.

84. **image190** — used to show how something like a graph, where each trend line has a different color, try relying on contrast instead of using completely different colors. It’s much easier for someone who’s colorblind to tell the difference between light and dark than it is for them to tell the difference between two distinct colors. show this image with image 191 only if there are graphs with colors on the page.

85. **image191** — used to show how It’s much easier for someone who’s colorblind to tell the difference between light and dark than it is for them to tell the difference between two distinct colors. show image only with image 190.

86. **image205** — used to show how a small button drop shadow makes a difference. A small drop shadow makes a button feel slightly raised off the surface, inviting interaction without being dramatic. show image if buttons dont have shades.

87. **image206** — used to show how a medium shadow on a dropdown or popover makes a difference. A medium drop shadow places a dropdown or popover clearly above the page while keeping it connected to it. show image only if dropdowns dont have shades.

88. **image207** — used to show how a large shadow on a modal makes a difference. A large drop shadow on a modal communicates maximum elevation and that background content is blocked. show image only if there are modal dialogs without shades.

89. **image219** — used to show how a card overlapping its container boundary makes a difference. A card overlapping its container boundary creates a strong depth illusion and communicates a relationship between the two. show this only if there are no overlaping elements on the page. together with image 220.

90. **image220** — used to show how a card crossing a section boundary makes a difference. Offsetting a card to cross a section boundary visually connects the card to its containing section.

91. **image222** — used to show how overlapping avatar circles without border separation make a difference. Overlapping avatar circles without a border separation blur into each other at the overlap edge. show this image only if there are a 4+ avatars next to each other. together with image 223.

92. **image223** — used to show how a white border ring on overlapping avatars makes a difference. A 2px white border ring on each avatar creates a clean visible edge wherever circles overlap.

93. **image224** — remove

94. **image225** — used to show how text placed directly on a photo without an overlay makes a difference. White text placed directly on a photo is readable over dark areas but completely invisible against bright ones. show this only if there is such text. show this with image227, 228, 229, 230.

95. **image227** — used to show how a dark overlay on a photo improves text contrast. A semi-transparent dark overlay levels the photo's brightness so white text maintains consistent contrast everywhere.show this only if there is such text. show this with image227, 228, 229, 230.

96. **image228** — used to show how a text-shadow on photo text makes a difference. A subtle text-shadow creates local contrast against both light and dark photo areas without darkening the whole image.show this only if there is such text. show this with image227, 228, 229, 230.

97. **image229** — used to show how desaturating and tinting a background photo makes a difference. Desaturating and tinting a background photo with a brand colour makes it cohesive and easier to overlay text on.show this only if there is such text. show this with image227, 228, 229, 230.

98. **image230** — used to show how combining desaturation and text-shadow on a photo makes a difference. Combining desaturation, reduced contrast, and a text-shadow gives readable text over any photo regardless of its content.show this only if there is such text. show this with image227, 228, 229, 230.

99. **image233** — used to show how scaling an icon up to 3x or 4x their intended size makes a difference. They lack detail, and always feel disproportionately “chunky”. show this only if there are icons with 40+px size. together with image234 and image235.

100. **image234** — used to show how placing a small icon inside a larger coloured container makes a difference. Keeping the icon at its native small size inside a bigger coloured container provides visual footprint without distorting the icon. shown together with image235

101. **image235** — used to show how the icon-in-container technique scales independently. The icon stays at its native size while the coloured container scales independently to whatever size is needed.

102. **image240** — remove 

103. **image241** — remove

104. **image242** — used to show how variable-height card images break grid alignment. Portrait photos at their natural aspect ratios make some cards much taller than others, breaking grid alignment. show this only if there are user-uploaded images displayed in some elements. together with image243.

105. **image243** — used to show how fixed-aspect-ratio containers keep a card grid uniform. Fixed-aspect-ratio CSS containers keep all cards the same height regardless of the original photo dimensions. only if image243 is shown.

106. **image248** — used to show how small accent details transform default-styled components. A coloured accent border, a custom bullet icon, or a styled selection state can each add character with a single CSS change. show only if there are basic bullet points in page. together with image249.

107. **image249** — used to show how custom icon bullets transform a plain list. Custom icon bullets replace default browser dots and add personality without structural changes.

108. **image250** — used to show how an oversized decorative quote mark transforms a testimonial. An oversized decorative quotation mark transforms a plain testimonial text block into a designed component. show only if there are testimonials in the page.

109. **image251** — used to show how Links are another great candidate for special styling. You can do something as simple as changing the color and font weight, or something as fancy as a thick and colorful custom underline that partially overlaps the text. show only if there are links in the texts with no styling.

110. **image252** — used to show how on a form, using custom checkboxes and radio buttons is an easy way to add some color to the design. shown only if there is checkboxes and radio buttons with no styling in the page.

111. **image258** — used to show how a contrasting background fill separates panels without borders. A contrasting background fill alone is enough to visually separate a panel from adjacent content without borders or shadows. show if there are a 4+ elements/containers/cards on the page with basic white background.

112. **image259** — used to show how a subtle gradient background adds depth to a flat panel. A subtle top-to-bottom gradient adds visual energy and perceived depth to what would otherwise be a flat panel. show this if the page has 3+ same background-colored sections.

113. **image270** — used to show how vertical spacing between list items replaces divider lines. Generous vertical spacing between list items does the structural work of a divider line without adding visual noise. show only if there are cards/containers with listed items. together with image273.

114. **image273** — used to show how vertical spacing between page sections replaces divider lines. Using box shadow,differnet background colors, and bigger spacing is a great way to create distinction between
groups of elements without introducing any new UI at all. show this only if image270 is shown.

115. **image274** — remove

116. **image275** — remove

117. **image276** — remove

118. **image277** — used to show how a small high-contrast CTA button counterbalances a large hero photo. A small high-contrast CTA button can counterbalance a much larger hero photograph in a split layout. shown also image 281, 282. show image if there is visual imbalance issue.

119. **image278** — remove

120. **image279** — remove

121. **image280** — remove

122. **image281** — used to show Without balance we create visual tension that can easily have a negative impact on how our designs are perceived by others. The image shown appears unbalanced. The centered text establishes a symmetrical balance on a vertical axis. However, the left side of the page is visually “heavier,” which creates visual tension. This visual tension causes our brains to think “something seems off about this design.”. show this image only if there is maybe visul imbalance. together with 282 and 277

123. **image282** - used to show balance of elements of texts and buttons. shown only if image281 is shown.